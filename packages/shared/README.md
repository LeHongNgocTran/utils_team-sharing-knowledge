# Shared Package

Small utilities shared by modules and the orchestrator:
- JSON file IO
- run id creation
- slug generation
- simple numeric helpers

Keep this package lightweight. Domain contracts belong in `packages/schemas`, and workflow behavior belongs in `packages/core`.
